# ──────────────────────────────────────────────
# InventarioGP — Frontend Environment Variables
# ──────────────────────────────────────────────
# COPIA este archivo como .env y completa los valores reales
# NUNCA comitees .env al repositorio

# URL base de la API (solo si el backend está en otro dominio)
# En desarrollo con proxy, dejar vacío
# En producción, ej: https://api.inventario.tudominio.com
# VITE_API_URL=
